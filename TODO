* Add achievements hints (on tabard, food, npc)
* Add bank and mail frames for hint
* Add checkbox for confidential data (money, etc.)
* Change language system, use english by default and source language,
if the foreign message is not found then use english message.
* Add for each entity a graphic view (frame)
* Add icons to delete and activate entity's items
